# STATUS — Tesla Parts Center

Оновлено: 2026-09-16. Продакшн-деплой: `main` = `feature/improvements` (див. `git log -1`).

## URL та доступ
- Shop: https://teslapartscenter.com.ua
- Admin: https://admin.teslapartscenter.com.ua (admin / admin123)
- API: https://api.teslapartscenter.com.ua (+ `/api/` same-origin проксі на домені магазину)
- Сервер: `ssh dmytropshenichnikov` → `/var/www/tesla-parts` (docker: gateway, shop, admin, backend, db)

## Правила проєкту
- Категорії каталогу — джерело істини. Схеми (EPC) і товари привʼязані до категорій/підкатегорій.
- **Ніколи не змішувати** «Model 3» і «Model 3 Highland» (та інші покоління) ні в каталозі, ні в схемах.
- Деплой — тільки через `./deploy/deploy.sh` (перевіряє гілку, чистоту дерева, сам мерджить `feature/improvements` у `main`).
- Ручні (непривʼязані) варіанти показують «Немає в наявності» без ціни й кошика; привʼязані беруть ціну/наявність з каталогу.
- Локальний скрипт вивірки: Playwright + системний Chrome (див. нижче).

## Виправлення 16.09 (записами клієнта)
- Схема більше не підтягується на чужу модель: схемu шукаються строго в межах своєї
  категорії (`model-options`, `sections`, `for-subcategory`, `subsystem-info`).
  Model 3 ≠ Model 3 Highland. Коміт `70fa2c2`.
- Шлях до схеми в магазині живе в URL: авто → розділ → підсистема → схема.
  Кнопка «Назад» (браузерна і на сторінці) повертає на попередній крок; крок
  «підсистема» показується завжди. Коміт `48e29b9`.
- В адмінці список схем отримав той самий шлях, що й каталог: авто → розділ →
  підсистема (з підкатегорій каталогу) → схема. Коміт `70d2681`.

- Порядок у схемах = порядок каталогу: моделі (`sort_order DESC`), розділи й підсистеми —
  за порядком підкатегорій каталогу, а не «за кількістю схем». Коміт `bc39e01`.
  В адмінці додано видиму ручку перетягування і підказку. Коміт `c3c30e1`.

- Крихти схеми без дублю назви вузла і з посиланнями на кроки. Коміт `b4f515d`.
- Картки розділів і підсистем у схемах — з великою картинкою (як у референсі клієнта).
  Коміт `d3a6e2f`.

- Наведення синхронізує схему та список деталей в обидва боки (номер ↔ деталь),
  підказка різна для десктопа й телефона. Коміт `8510de5`.
- У каталозі на сторінці підкатегорії — червона кнопка «Переглянути на схемі»
  (блок «Схема цього вузла»). Коміти `8510de5`, `a5f6b1c`.
- Множини: «1 деталь / 2 деталі», «1 точка / 2 точки».

- Сторінка більше не «відкривається знизу»: скидаємо прокрутку не лише при зміні
  шляху, а й при зміні query (кроки схем живуть у query) + підстраховка після рендера.
  Коміт `e9afdf5`.

- Мінус один клік: підсистема з єдиною схемою відкриває її одразу (`/schemes/{id}`).
  Коміт `5745e73`.
- Тач: hover-стилі вмикаються лише з курсором (`future.hoverOnlyWhenSupported`),
  тому картки на телефоні не виглядають «вже обраними». Коміт `5745e73`.
- Крок «розділ» показує «Завантаження розділів…», а не помилкове «схем ще немає».

- Зі сторінки схем прибрано блок «Аксесуари та універсальні схеми» — лишились
  тільки моделі авто. Коміт `a93aeec`.

- Порядок схем: в адмінці перетягування картки + стрілки ↑↓, новий ендпоінт
  `POST /schematics/reorder`, магазин показує схеми в цьому ж порядку. Коміт `001a065`.
- Глибокі посилання виду `?model=Model 3&generation=Highland` більше не падають на
  базову модель (виправлено пріоритет варіант-категорій). Коміт `bf8a6a5`.

- ГОЛОВНИЙ ФІКС адмінки: варіант товару більше не губиться. `handleUpdateHotspot`
  писав стан із застарілого знімка, тому другий виклик в одному обробнику
  (додати варіант + автозаповнити парт-номер) перетирав перший. Коміт `e62667f`+`0f7b4b9`.
- Додавання варіанта: підсвітка і прокрутка до нового; у пошуку позначка «Вже додано»;
  назва точки більше НЕ перейменовується на назву товару.
- Порядок варіантів — стрілки ↑↓ (порядок зберігається у variants_json і так само
  показується в магазині). Коміт `e62667f`.

- Магазин, сторінка схеми: жодну точку не вибираємо й не розкриваємо автоматично —
  усе біле, поки користувач сам не натисне номер (клієнт просив саме так). Коміт `32aeac0`.

- Зум креслення на сторінці схеми (як в оригінальному каталозі): кнопки +/−,
  скидання, відсоток, до 800%, коліщатко (Ctrl/пінч), подвійний клік, перетягування
  для панорамування, кнопка «на весь екран». Точки деталей масштабуються разом із
  кресленням. Коміт `f240d13`.

- Керування видимістю категорій у схемах: поле `category.show_in_schematics`,
  перемикач «око» в «Категоріях», ендпоінт `POST /categories/{id}/schematics-visibility`.
  Магазин показує лише увімкнені категорії; «Аксесуари» типово вимкнені. Коміт `df2da09`.
- Адмінка, EPC: схеми показуються лише після вибору розділу/підсистеми (є кнопка
  «Показати всі схеми розділу»), щоб порядок випадково не змінювався серед усіх схем.

- Картки розділів/підсистем: автокроп порожніх полів + вибілювання фону
  (`utils/trimImage.ts`), тож сірої плашки навколо малюнка більше немає. Коміти
  `618ac5a`, `9db8d87`.

- Кошик: товар, доданий зі схеми, показує ФОТО товару з каталогу (бекенд віддає
  `image` у варіанті, магазин його бере), назва без дублювання. Коміт `7791f0c`.

- Тип деталі зі схеми тепер визначається тим самим правилом, що й на сайті
  (поле part_type → назва → опис): у кошику показується «Аналог», як на сторінці
  товару. Коміт `c4a3eb1`.

- Наближення креслення: показуємо межу різкого збільшення (для файлу 1414px у
  вікні 655px це 216%) — далі відсоток стає янтарним із поясненням. В адмінці при
  завантаженні схеми підказка «від 2000 px» і попередження, якщо файл дрібніший.
  Коміт `f2c9dbe`. Конвеєр картинки НЕ стискає (перевірено: немає ресайзу, ліміт 20 МБ).

- В адмінці схем: завантаження креслення ЗА ПОСИЛАННЯМ (пряме посилання на картинку
  з EPC Tesla) — бекенд качає оригінал без стиснення, перевірено: MD5 і розміри
  збігаються з джерелом. Плюс підказка, до якого масштабу буде різко. Коміт `4095363`.

- Перевірено: epc.tesla.com віддає **403** програмним запитам (і з нашого сервера теж),
  тому «взяти за посиланням» для Tesla не працює — додано зрозумілу підказку.
- Робочий шлях без втрати якості: скопіювати картинку в браузері → **⌘V/Ctrl+V** в
  адмінці (файл іде з браузера, зберігається оригінал). Коміт `5d4d459`.

- ВИПРАВЛЕНО: два швидкі кліки по «+» більше не скидають масштаб на 100%.
  Причина — dblclick з кнопки тулбара долітав до полотна і викликав скидання.
  Тепер dblclick з кнопок не проходить, а по самому кресленню працює як раніше.

- Деталі з однаковим номером на кресленні (напр. два «7») показуються одним
  рядком у списку: обидві точки підсвічуються разом, лічильники рахують позиції.
  Коміти `7d69e5c`, `b1c0f5e` (хуки групування підняті вище ранніх return).

- Консоль чиста: (1) React #310 — був мій баг з хуками після раннього return, виправлено;
  (2) CORS-помилки картинок — кеш без заголовків, тепер завантаження з версією + фолбек;
  (3) Tailwind CDN більше не використовується — CSS збирається на білді (65 КБ, 10.6 КБ gzip).
  Коміт `c14b5a9`. Перевірка: 5 сторінок, 0 помилок/попереджень у консолі.

- ВИПРАВЛЕНО: картинки розділів/підсистем у схемах більше не беруться з чужих
  категорій. Причина була подвійна: шукали за назвою по всьому каталогу, а для
  варіанта (Highland) ще й підставляли базову категорію (Model 3). Коміти
  `f5c4731`, `1a4c0e2`. Перевірка: сторінка Highland вантажить лише свої картинки.

- Адмінка теж переведена на збірку Tailwind (без CDN) — попередження зникло,
  стилі перевірені на сторінках Дашборд/Категорії/Товари/Схеми. Коміт `8d2784d`.
- Прибрано сміття серед картинок: 152 точні дублікати видалено (~110 МБ),
  416 файлів без посилань у БД перенесено в `/var/www/tesla-parts/_unused-images/`
  (~1.25 ГБ, можна відновити). Перевірено: 0 битих картинок і 0 помилок 404
  на всіх ключових сторінках магазину й адмінки.

- Схема: клік по ВСЬОМУ рядку деталі відкриває її (раніше працював лише
  кружечок). Вибрана точка на кресленні тепер біла з товстим червоним контуром,
  більша і пульсує — не сплутати з рештою. Коміт `fb9b6e4`.

- ВИПРАВЛЕНО: автовідкриття єдиної схеми більше не закидає вперед після «Назад»
  (спрацьовує лише коли користувач сам обрав підсистему). Плюс картинки в картках
  показуються без миготіння «сіре → біле» (беремо з кешу обробки). Коміт `4286b7c`.
- Адмінка: картинки категорій/підкатегорій обробляються ПЕРЕД завантаженням
  (кроп + вибілювання фону), креслення схем не чіпаємо (координати точок!).
  Перевірено: 800×400 із сірим фоном → збереглось 350×183 із білим фоном. Коміт `6fb313d`.

- Кнопка «Назад» на кроці схем тепер іде КАСКАДОМ по кроках (підсистема → розділ →
  вибір авто), а не в історію браузера — тож перехід із зовнішнього посилання
  не викидає зі схем на попередню сторінку сайту. Коміт `9b0f4a1`.

- Чип обраного авто («Tesla Model 3 Highland») тепер велика кнопка й веде на
  вибір авто — на тачскріні по ньому легко влучити. Коміт `a5f3c0b`.
- Картинки в картках показуються вже обробленими (без «сіре → біле»): перевірено
  на холодному завантаженні — 0 кадрів з оригіналом.
- На сторінці схеми додано велику кнопку «До списку схем» — повертає на
  попередній крок (розділ/підсистему). Коміт `9f2b7d1`.

- Миготіння «сіре → біле» прибрано НАЗАВЖДИ: обробку картинок (кроп + білий фон)
  перенесено на сервер (Pillow) — при завантаженні в адмінці + разовий прохід по
  336 наявних картинок категорій/підкатегорій (309 змінено, бекапи в
  `_unused-images/pre-trim`, 62 МБ). Магазин більше нічого не перемальовує.
- Формати виправлено: 123 файли мали PNG-дані з розширенням .jpg — перекодовано,
  Content-Type тепер збігається. Коміти `b4e2f11`, `c7d5a94`, `f1a0c33`.

- Адмінка, нова схема: форма створюється ПОРОЖНЬОЮ — прибрано залишки
  «НАРУЖНЫЕ КРЕПЛЕНИЯ / Защита днища и диффузор»; категорію беремо з фільтра
  списку (Model Y → схема створюється під Model Y, а не під Highland); зміна
  категорії скидає розділ/підсистему й перезавантажує список з каталогу.
  Коміти `2c9e7a1`, `d4b6f80`.

- Крихта обраного розділу тепер із «×» і підказкою «Обрати інший розділ», а на
  кроці підсистеми є посилання «Змінити розділ». У відео клієнт саме натискав назву
  розділу в крихтах (вона його скидала) — тепер це очевидна дія, а не «зникає розділ».
  Коміт `7d3c1a8`.

- Чип авто веде на вибір РОЗДІЛУ цього ж авто (не до списку всіх машин):
  авто лишається вибраним, скидаються тільки розділ/підсистема. Зміна авто —
  окремим посиланням «Змінити авто». Коміт `3e5b1d9`.

- Телефон: сторінка більше не «стрибає» вниз після вибору авто/розділу. Причина —
  зміна висоти верстки, коли картинки карток догружались (iOS підтягував прокрутку
  до раніше видимого елемента). Коміт `4c8f2e6`: картинки в картках мають фіксовану
  висоту, `overflow-anchor: none`, і скидання прокрутки кілька разів, поки крок рендериться.

- ВИПРАВЛЕНО «Failed to upload schematic image»: мій попередній патч випадково
  видалив `formData.append('file', file)` у завантаженні фото схеми (і у відгуках) —
  запит ішов без файлу → бекенд відповідав 422. Плюс в адмінки nginx різав файли
  понад 1 МБ (не було `client_max_body_size`) → великі креслення не проходили.
  Коміти `7a4c9e2`, `c1d8f04`. Перевірено: вставка 1.27 МБ → 200, 10 МБ → 200.

- Картки підкатегорій у каталозі більше не ріжуть малюнок: було `object-cover`
  (обрізав по краях), стало `object-contain` — малюнок видно повністю. Причина:
  після обрізання полів на сервері `cover` дорізав сам малюнок. Коміт `b6e2a91`.

- Картки підкатегорій у ряду тепер однакової висоти (`h-full`): раніше довгі назви
  (3–4 рядки) розтягували картку, і ряд «ламався» (194 / 166 / 158 px). Коміт `c7f2a05`.

- **ВІДКАЧЕНО автообрізання картинок** (коміти `64b1456`, `9f...`): обробка різала
  світло-сірі частини креслень (друга туманка, тонкі лінії) — вони приймались за фон.
  Повернуто 308 оригіналів підкатегорій + 1 категорії з `_unused-images/pre-trim`,
  обробку вимкнено повністю (картинка показується рівно та, що її завантажив адмін).
- Виправлено 2 картинки категорій (Model S, Model 3 Highland): усередині був PNG,
  а розширення `.webp` → пересохранено як справжній WebP; у backend додано MIME-типи
  `.webp/.avif/.heic/.svg` (до цього віддавався `application/octet-stream` і плитка
  не показувалась узагалі).
- Теку `products` і `schematics` обробка НЕ чіпала (0 файлів) — перевірено по mtime.

- **Нова схема підтягує шлях з фільтрів**: якщо у списку схем обрано модель →
  розділ → підсистему, то у формі створення ці три поля вже стоять на своїх місцях
  (раніше розділ був «— оберіть розділ —», а підсистема порожня). Перевірено на
  живому адмінi реальним браузером: Model 3 → ЗОВНІШНЄ ОЗДОБЛЕННЯ → ЗАХИСТИ ПЕРЕДНІ.

- **Перетягування товарів (варіантів) у вузлі схеми**: у блоці «Варіанти наявності
  та цін» рядки тепер можна міняти місцями мишкою (за рядок або за ручку-грип),
  а не тільки стрілками. Перевірено живим браузером на проді: у вузлі #1 схеми
  «ЗАХИСТИ ЗАДНІ» порядок 113541000A ↔ 110083800C змінився перетягуванням;
  збереження не робив, тому порядок у БД лишився початковим.

## Зв'язка VIN ↔ схеми ↔ гараж (зроблено в цьому циклі)
- **Пошук за номером авто**: коли номера немає в реєстрі або зовнішній сервіс
  відповідає помилкою, показуємо зрозуміле «не знайдено + введіть VIN» замість
  «сервіс недоступний», і **автоматично перемикаємо на вкладку VIN** (і на
  сторінці гаража, і в модалці з шапки).
- **Каталог підлаштовується під авто з гаража**: плитка своєї моделі стає першою
  з бейджем «Ваше авто», у списках товарів зʼявилась панель «підходить N з M»,
  фільтр «Тільки для мого авто» і зелена позначка «Ваше авто» на сумісних
  позиціях (`utils/garageMatch.ts`).
- **Гараж прив'язаний до акаунта**: таблиця `garagecar` + ендпоінти
  `GET/POST/PATCH/DELETE /garage` та `POST /garage/import`. Гараж із браузера
  переноситься в акаунт при вході (ідемпотентно, без дублів), авто їде за
  покупцем на інший пристрій; localStorage лишається кешем і режимом для гостей.
- Перевірено живим браузером: гість додає авто → вхід → авто в акаунті →
  чистий браузер з тим самим акаунтом бачить його без повторного введення.

- **Навігація зі схеми назад**: кнопка «До списку схем» робила `navigate(-1)`,
  тобто крок у ДОВІЛЬНЕ місце історії — зі схеми можна було вилетіти аж на вибір
  моделі. Тепер це фіксоване посилання «До підсистем розділу» (крок, де
  обирають «ЗАХИСТИ ПЕРЕДНІ / ЗАХИСТИ ЗАДНІ»), поруч — «Усі схеми розділу».
- **Перемикання підсистем прямо на схемі**: під хлібними крихтами зʼявився рядок
  «Підсистеми розділу» з сусідніми вузлами — перехід на іншу підсистему тепер
  один клік, без повернення в список.

- **Вихід з акаунта чистить гараж із браузера** (і при 401 так само): авто
  належать акаунту, тож на спільному пристрої наступна людина їх не побачить.
  Очищення живе в `AppContext` (провайдер змонтований завжди), а не на сторінці
  гаража — інакше подія не мала слухача, коли людина виходила з іншої сторінки.
- **Суцільний аудит вимог на проді** (живий браузер + API):
  - пошук за номером: неіснуючий номер → 404 + перехід на вкладку VIN з
    поясненням; реальний не-Tesla номер → 400 «не є Tesla»; VIN розшифровується;
  - каталог: плитка авто перша з бейджем, панель «підходить N з M», фільтр
    звужує список (16 → 14), чипи на сумісних товарах;
  - гараж: 401 без токена, дубль VIN не створюється, чуже авто → 404, видалення
    активного передає активність наступному, чистий браузер із тим самим акаунтом
    бачить авто (2 з 2), додавання через модалку в шапці теж пише в акаунт,
    гість працює локально й у БД не потрапляє.

- **Зі схеми можна відкрити сам товар**: назва деталі в списку «Деталі вузла» —
  посилання на картку товару (і для точки з одним варіантом, і для кожного
  варіанта окремо). Привʼязка та сама, що для кошика: `product_id` варіанта.
- **З товару видно схему**: під кнопкою «Купити / Додати в кошик» зʼявився
  компактний блок «Ця деталь на схемі — <вузол> та ще N» з переходом на схему, а
  повний блок «Є на схемах» переставлено ВИЩЕ опису деталі.
- **Картка товару почищена від дублів**: прибрано бейджі «В наявності»/«Аналог»
  над назвою (вони вже є на фото) і нижній рядок «Відправка Новою Поштою по
  Україні / Гарантія 14 днів» (дублював блок «Гарантія / Нова Пошта / Оплата /
  Швидка відправка» праворуч).

- **Ще один дубль на картці товару прибрано**: блок «Доставка та оплата»
  (Нова Пошта / Способи оплати / Гарантія повернення) повторював плитки під
  кнопкою купівлі — лишилися тільки плитки, а «Технічні характеристики» тепер
  на всю ширину. На сторінці згадок «Нова Пошта» стало 1 замість 4.

- Гараж більше не нав'язує модель у схемах: показуємо всі 6 карток авто + підказку
  «Ваше авто з гаража: … [Показати схеми для нього]». Можна дивитись схеми будь-якої
  машини, не змінюючи гараж. Скидання авто чистить і стан, і URL. Коміт `0453f48`.

- Сторінка товару: блок піднято вище — менші верхні відступи (md 32→20px), прибрано
  порожній рядок тегів моделей на десктопі, менший відступ під кнопкою «Назад».
  Заголовок на 32px вище, фото — на 12px. Коміт `e274037`.

- Схема на сторінці товару відповідає моделі, з якої прийшли: каталог передає
  `?model=`, кнопка «Ця деталь на схемі» веде на схему цієї моделі й підписує її,
  а якщо схеми цієї моделі немає — чесне попередження + бейджі «інша модель».
  Коміти `ccb1ce5`, `640e4bd`.

- Кнопка «Ця деталь на схемі» веде ЛИШЕ на схему обраної моделі: для Model Y
  (схеми ще немає) замість чужої схеми показуємо нейтральне повідомлення, а схеми
  інших моделей лишаються в блоці «Є на схемах» з бейджем «інша модель».
  Для базової Model 3 веде на базову схему, а не на Highland. Коміт `8421847`.

- Крихти схем: клік по назві розділу повертає РІВНО на крок назад — до списку
  підсистем цього ж розділу (раніше скидав і розділ, тож доводилось тицяти двічі).
  Коміт `e0f1b13`.

- Адмінка → «Товари в підкатегорії»: порядок товарів можна міняти перетягуванням
  рядка (ручка ⋮⋮ + підказка), стрілки ↑↓ лишились. Коміт `8930b67`.

- Прибрано «×» у чипа розділу (крихти схем): вкладка більше не виглядає закриваною,
  клік по ній працює як і раніше — крок назад до підсистем. Коміт `e1a2e71`.

- Чип розділу в крихтах схем став статичною позначкою (span): не клікається,
  нічого не скидає й не «закриває». Змінити розділ — «Змінити розділ» або «Назад».
  Коміт `39a097c`.

- Адмінка → Категорії: «око» тепер показує кількість схем (Model 3 — 3, Model Y — 2)
  і неактивне, якщо схем немає (Model S/X/Juniper/Аксесуари — 0). У підказці прямо
  сказано, що каталог воно не ховає. Коміт `17d3f54`.

- Чип розділу в крихтах схем тепер ЗАВОДИТЬ у розділ (показує його підсистеми),
  а не закриває його — так само, як чип моделі заводить у модель. Коміт `e23fa8c`.

- Фікс битого посилання «all»: «Усі схеми розділу» тепер через `all=1` (було
  `subsystem=all`, через що в крихтах світилось «all» і «Схем не знайдено»).
  Плюс захист: невідома підсистема в URL скидається на крок вибору підсистем.
  Коміт `d376ebb`.

- Перетягування у варіантах схеми та в «Товарах в підкатегорії» тепер працює
  ЛИШЕ за ручку ⋮⋮ — текст у полях знову виділяється й копіюється (раніше
  виділення запускало перетягування рядка). Коміт `0aa58b3`.

- Промокоди: статус в адмінці тепер текстом («Увімкнено» / «Вимкнено», клікабельний),
  коди шукаються без урахування регістру й пробілів, повідомлення українською.
  SERVICE5 був вимкнений — увімкнув, знижка 5% працює (230 → 218 грн). Коміт `9a84a7c`.

- Чипи в крихтах схем виглядають однаково: у чипа моделі тепер такий самий
  шеврон › (був ←), однакові відступи й висота 42px. Коміт `f7796b4`.

- Перемикач «показувати у схемах» переїхав у редагування категорії (олівець):
  чекбокс із поясненням і кількістю схем; з рядка «око» прибрано. Коміт `8a4b596`.
- ГОЛОВНИЙ ФІКС: `GET /categories` не віддавав `show_in_schematics`, тому адмінка
  завжди показувала «увімкнено» навіть коли модель прихована. Коміт `9f3b7c6` (schemas).

- Перемикач схем працює незалежно від наявності схем: можна сховати/показати картку
  моделі навіть якщо схем у неї ще немає (перевірено на Model Y Juniper з 0 схем).
  Коміт `c8f0d21`.

- Деталі вузла можна згортати: клік по номеру тепер перемикає (розкрити ↔ згорнути),
  кнопка рядка «Обрати» ↔ «Згорнути», у заголовку — «Згорнути все». Коміт `7c2cc5a`.

- Блок схем на сторінці розділу каталогу: замість стосу великих карток —
  компактний рядок, що гортається (картка 70px, було ~120px), + посилання
  «Усі схеми розділу». Одна схема — як і раніше, широка картка. Коміт `86cc3d1`.

- Поле «Номер на схемі» можна повністю очистити й вписати новий номер (було
  `type=number` + `parseInt()||1` — «1» не стиралась, виходило «17»). Тепер
  текстове поле з цифровою клавіатурою, порожнє значення повертає попереднє.
  Коміти `a37c1e9`, `f2d8c05`.

- Вибір товару з каталогу: назва показується у два рядки (було обрізано в один),
  модалка ширша (576 → 672px) — повні назви видно. Коміт `f342c96`.

- Мобільний: у кожному розкритому списку деталі зʼявилась явна кнопка «Згорнути»
  (+ кнопка «Згорнути все» у заголовку). Перевірено тапами в емуляції iPhone. Коміт `7fcb092`.

- При згортанні списку деталі знімається позначка вибору: рядок і номер точки
  повертаються в нейтральний стан (працює і для «Згорнути», і для «Згорнути все»).
  Коміт `840b473`.

- VIN: коди мотора тепер за моделлю — Model Y з кодом K дає «Dual Motor AWD»,
  Plaid лишається тільки для Model S/X (код 6), Cyberbeast — лише Cybertruck.
  4-й символ C = Cybertruck. Авто в гаражі самі оновлюють опис за VIN.
  Коміти `0e2c4d1`, `b7f1a90`, `e8e3b7e`.

- Гараж: блок «ваш парк» показується вже з 1 авто (був тільки при 2+) —
  з одним авто зникало і саме авто, і видалення. Коміт `c054ed1`.

- Гараж/модалка: видалення одного авто більше не «зносить» все — видаляється
  ТІЛЬКИ натиснуте, решта лишається; перше з решти стає активним скрізь
  (модалка, сторінка гаража, каталог/схеми). Плюс список «Інші авто в гаражі»
  в модалці. Коміт `8741fff`.

- Гараж/акаунт: перевірено, що авто з акаунта (2 шт. у customer_id=2) підтягуються
  на чистому браузері з токеном. Додано плашку «У цьому браузері авто ще не
  завантажені» з кнопкою «Оновити» та показом помилки синхронізації — щоб
  порожній гараж на новому пристрої був зрозумілим. Коміт `8005397`.

- Гараж: авто, додане з іншого пристрою, підтягується без очікування — при
  відкритті сторінки гаража і навіть при поверненні на вкладку (focus/visibility).
  Перевірено: телефон зі старим кешем (2 авто) побачив 3-тє (з ПК) і 4-те
  (додане під час тесту) без перезавантаження. Коміт `6c1d41b`.

- Мобільне бургер-меню: авто з гаража видно одразу під пунктом «Мій гараж»
  (модель, рік, покоління, номер; активне — червоним). Тап по авто робить його
  активним і веде в схеми — без заходу в гараж. Коміт `631aa02`.

- Бургер-меню синхронізує список авто з акаунтом при відкритті (тротл 60с):
  нове авто з іншого пристрою зʼявляється в меню без заходу на сторінку гаража.
  Перевірено: кеш 2 → 8, дублів не створюється. Коміт `e2a3d9f`.

## Зроблено (актуальний цикл)
- Картки вибору авто на сторінці схем тепер як на головній каталогу: великі фото 3-в-ряд,
  градієнт, назва моделі, «Переглянути схеми →», бейдж кількості схем.
- У навбарі біля «Каталог» додано іконку (Grid2x2, tesla-red) — desktop і mobile.
- Перевірено на проді: крок 1 (6 карток), крок 2 (розділ), крок 3 (вузли + деталі вузла з цінами),
  порожній стан для моделі без схем, мобільна верстка 390px. Помилок консолі немає.

## Команди вивірки
```bash
cd tesla-parts-shop      && npx tsc --noEmit && npm run build
cd tesla-parts-admin     && npx tsc --noEmit && npm run build
cd tesla-parts-backend   && python3 -m py_compile main.py routers/*.py
# деплой на сервері
ssh dmytropshenichnikov 'cd /var/www/tesla-parts && git fetch -q origin && git reset -q --hard origin/main && ./deploy/deploy.sh'
```

## Відкриті питання
- Наповнення схемами: зараз у БД один приклад (Model 3 Highland → ЗОВНІШНЄ ОЗДОБЛЕННЯ → ЗАХИСТИ ПЕРЕДНІ).

## Виправлення 21.09 — перейменування категорії «відвʼязувало» схему
- Симптом клієнта: перейменував підкатегорію «ПЕТЛІ ТА ФУРНІТУРА ДЛЯ ВХІДНИХ ДВЕРЕЙ»
  (Model Y → «ПЕТЛІ ТА ФУРНІТУРА ПЕРЕДНІХ ДВЕРЕЙ»), після чого схема 52 «зникла»
  в адмінці, а на сайті лишилась під старою назвою.
- Причина: схеми зберігають model/generation/section/subsystem ТЕКСТОМ
  (без category_id), а `PUT /categories/{id}` і `PUT /subcategories/{id}` міняли
  лише імʼя в каталозі. Фільтр адмінки `GET /schematics?model=<нова назва>`
  повертав 0, а сайт групує дані з тексту самих схем — тому показував старе.
- Каскад у бекенді (`routers/schematics.py`: `cascade_category_rename`,
  `cascade_subcategory_rename` + `_category_scope_conditions`; викликаються
  з `update_category`/`update_subcategory` ДО присвоєння нового імені):
  перейменування категорії оновлює model/generation схем (4 гілки: база→база,
  варіант→варіант із заміною суфікса в поколінні, база→варіант, варіант→база),
  перейменування підкатегорії — section (верхній рівень) або subsystem (діти),
  строго в межах своєї категорії. Нове імʼя класифікується без старого
  (пастка «КУЗОВ»→«КУЗОВ НОВИЙ», коміт `9bca34c`). Коміт `e0ca571`.
- Схему 52 полагоджено: subsystem і title — під нову назву
  («Петлі та фурнітура передніх дверей»), хотспоти не чіпались.
- Перевірено на проді: E2E-скрипт на тимчасових рядках (категорія+розділ+
  підсистема+схема ZZZ, приховані від магазину) — 20/20 PASS: каскад у всі
  три поля, чужі схеми не чіпаються, прибирання без залишків (404/404/[]).
  Скан усіх 35 схем: розбіжностей 0. `for-subcategory/272` знову віддає схему 52.
  Playwright: шлях Model Y → КОМПОНЕНТИ ЗАКРИТТЯ → нова підсистема →
  /schemes/52 з новим заголовком, старої назви немає, помилок консолі 0.
- Відоме обмеження (не чіпали): ПЕРЕНЕСЕННЯ підкатегорії в іншу категорію/розділ
  каскаду не має — привʼязку треба поправити в редакторі схеми. Також
  `DELETE /schematics/{id}` не видаляє рядки хотспотів (сироти в БД).
